pipeline:
    build:
        image: alexgian/alpine-compiler
        commands:
            - cd Project4s && make

    test:
        image: alexgian/alpine-compiler
        commands:
            - echo "Hello World"
matrix:
  TESTFOLDER:
    - Project4s/
